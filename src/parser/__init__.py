from datetime import date
def construct_feedback(
    s: str, today: date | None = None) -> date:
    """Given the input string and today date, compute the desired output. The output is the desired date in the format of date from datetime. If no input date is given should defaut to the curent date.
    Example
    --------
    >>> construct_feedback(s = 'five days from today', today = date(2023, 10, 6)
    2023-10-11
    """

def main() -> None:
    print("Hello from parser!")
